{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "node-terminal",
            "name": "Run Script: build (dns.chrisshop.com)",
            "request": "launch",
            "command": "npm run build",
            "cwd": "${workspaceFolder}"
        }
    ]
}